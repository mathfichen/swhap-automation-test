"""swhap-core — deterministic Layer-1 SWHAP workbench generator.

M1a slice: read-only archive inspection (``swhap inspect <archive> --json``).
No extraction to disk; member enumeration + crit-M6 rejection-rule evaluation
on the member list directly.
"""

__version__ = "0.1.0"

INSPECT_SCHEMA = "swhap-core/inspect/v1"
